<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    /* Custom Input Styles */
    .input {
      border: none;
      outline: none;
      border-radius: 15px;
      padding: 1em;
      background-color: #F2EBE3;
      box-shadow: inset 2px 5px 10px rgba(0,0,0,0.2);
      transition: 300ms ease-in-out;
      width: 100%;
    }

    .input:focus {
      background-color: #fff;
      transform: scale(1.05);
      box-shadow: inset 2px 5px 10px rgba(0,0,0,0.3);
    }

    /* Button Styles */
    .button {
      padding: 12px 22px;
      border-radius: 50px;
      border: none;
      color: #fff;
      cursor: pointer;
      background-color: #7d2ae8;
      transition: all 0.2s ease;
      width: 100%;
    }

    .button:hover {
      background-color: #6200ea;
    }

    .button:active {
      transform: scale(0.98);
    }

    /* Form Container Styling */
    .form-container {
      padding: 30px;
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
      max-width: 400px;
      width: 100%;
    }

    /* Heading Color */
    .form-heading {
      color: #5C6BC0;
    }

    /* Link Style */
    .form-link {
      color: #7d2ae8;
    }

    .form-link:hover {
      color: #6200ea;
    }
  </style>
</head>

<body class="bg-light" style="height: 100vh; margin: 0;" bgcolor="#F2EBE3">
  <!-- Centered Form Container -->
  <div class="container d-flex justify-content-center align-items-center" style="height: 100vh;">
    <form method="POST" action="/login" class="form-container">
      <h1 class="text-center form-heading mb-4">Login</h1>

      <!-- Username Input -->
      <div class="mb-3">
        <input type="text" name="username" class="input form-control" placeholder="Username" required />
      </div>

      <!-- Password Input -->
      <div class="mb-3">
        <input type="password" name="password" class="input form-control" placeholder="Password" required />
      </div>

      <!-- Submit Button -->
      <div class="mb-3">
        <button type="submit" class="button">Login</button>
      </div>

      <!-- Register Link -->
      <p class="text-center">Don't have an account? <a href="/register" class="form-link">Register</a></p>
    </form>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
